[PHP]
; ------------------------------------------------------------------------------
; Configuración General y Límites de Memoria
; ------------------------------------------------------------------------------
memory_limit = 256M
max_execution_time = 30
max_input_time = 60
post_max_size = 32M
upload_max_filesize = 32M
default_socket_timeout = 60

; ------------------------------------------------------------------------------
; Seguridad y Visualización de Errores (Producción)
; ------------------------------------------------------------------------------
display_errors = Off
display_startup_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
expose_php = Off
allow_url_fopen = On

; ------------------------------------------------------------------------------
; OPcache (Vital para el rendimiento en Producción / Koyeb)
; ------------------------------------------------------------------------------
[opcache]
opcache.enable = 1
opcache.enable_cli = 1
opcache.memory_consumption = 128
opcache.interned_strings_buffer = 16
opcache.max_accelerated_files = 10000
opcache.max_wasted_percentage = 10

; En producción no validamos si el archivo cambió en el disco (ahorra I/O)
opcache.validate_timestamps = 0
opcache.revalidate_freq = 0

; Optimización de lectura de scripts
opcache.save_comments = 1
opcache.fast_shutdown = 1

; Preloading de Laravel (Opcional, descomentar si usas preload en public/index.php)
; opcache.preload = /var/www/html/preload.php
; opcache.preload_user = www-data

; ------------------------------------------------------------------------------
; Sesiones (Optimizado para Livewire 3 y Redis / Colas)
; ------------------------------------------------------------------------------
[Session]
session.use_strict_mode = 1
session.use_cookies = 1
session.use_only_cookies = 1
session.name = SAMADHI_SESSID
session.cookie_httponly = 1
session.cookie_secure = 1
session.cookie_samesite = "Lax"
session.gc_maxlifetime = 7200

; ------------------------------------------------------------------------------
; Just-In-Time Compiler (JIT para PHP 8.2)
; ------------------------------------------------------------------------------
opcache.jit = tracing
opcache.jit_buffer_size = 32M
